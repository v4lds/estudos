algoritmo "ConteAte10"
var
   Contador: Inteiro
inicio
      Contador <- 50
      Enquanto (contador >= 1 ) faca
         Escreval (contador)
         contador <- contador - 1
      FimEnquanto
      Escreval (Contador)
fimalgoritmo